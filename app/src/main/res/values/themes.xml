<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PayScope" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PayScope" parent="Base.Theme.PayScope" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <!-- Customize your popup theme here -->
        <!-- Example customization -->
        <item name="android:colorBackground">@color/Primary_main</item>
        <item name="android:textColor">@color/black</item>
        <!-- Other popup theme attributes -->
    </style>
</resources>